name: "Deploy Example"

on:
  push:
    branches: ["master"]

jobs:
  deploy-examen:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Hacemos pull del código"
      - run: echo "Hacemos push del código"
      - run: echo "Compilamos"
      - run: echo "Deployamos"